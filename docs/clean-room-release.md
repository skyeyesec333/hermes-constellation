# Clean-Room Release

Public artifacts are compiled one way from an explicit allowlist into an empty directory. The release environment must not mount a private vault, home state, browser state, private environment variables, or shared private caches.

Every public file declares its lineage as handwritten generic code, independently fictional data, or public-source material. Exact built artifacts—not merely the working tree—must pass privacy and release-canary checks before publication.
